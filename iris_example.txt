import pandas as pd
from sklearn.decomposition import PCA
import numpy as np
import matplotlib.pyplot as plt

dat=pd.read_csv('/Users/PBalachandran/Dropbox/ML/data/iris.csv')

pcaDat=dat[['sepal_length', 'sepal_width', 'petal_length','petal_width']]
pcaClassVec=dat.species
C=list(set(pcaClass))
cl=['blue','red','green']

pca=PCA(n_components=2)

pca.fit(pcaDat)

Z=pca.transform(pcaDat)

for i in range(len(C)):
    plt.scatter(Z[np.where(pcaClass==C[i]),0],Z[np.where(pcaClass==C[i]),1],c=cl[i])

plt.legend(C)
plt.show()
